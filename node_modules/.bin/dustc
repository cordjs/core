#!/bin/sh
if [ -x "`dirname "$0"`/node" ]; then
  "`dirname "$0"`/node"  "`dirname "$0"`/../dustjs-linkedin/bin/dustc" "$@"
  ret=$?
else 
  node  "`dirname "$0"`/../dustjs-linkedin/bin/dustc" "$@"
  ret=$?
fi
exit $ret
